# Server Registry auditing rules (Registry2)
# For: win-registry2.ps1  (Apply to "Subkeys only")
# Format: id|path|perm|note

reg2_portproxy|HKLM:\SYSTEM\CurrentControlSet\Services\PortProxy|SetValue|PortProxy (subkeys only)

# High-value persistence on servers
reg2_w32time_providers|HKLM:\SYSTEM\CurrentControlSet\Services\W32Time\TimeProviders|SetValue|W32Time TimeProviders (subkeys only)
reg2_print_monitors|HKLM:\SYSTEM\CurrentControlSet\Control\Print\Monitors|SetValue|Print Monitors (subkeys only)

# Optional (often high-signal on servers too)
reg2_usbstor|HKLM:\SYSTEM\CurrentControlSet\Services\USBSTOR|SetValue,Delete,QueryValues|USB storage usage (subkeys only)
